// compressionUtils.ts
export function compress(content: string): string {
    // Implémentation factice de la compression
    const compressedContent = content.replace(/\s+/g, ''); // Supprime simplement les espaces pour illustrer la compression
  
    return compressedContent;
  }
  
// encryptionUtils.ts
export function encrypt(content: string): string {
    // Implémentation factice du cryptage
    const encryptedContent = content.split('').reverse().join(''); // Inverse simplement la chaîne pour illustrer le cryptage
  
    return encryptedContent;
  }
  